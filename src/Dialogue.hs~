module Dialogue where

import    System.Environment (getArgs)

dialogue :: IO ()
dialogue = do
  putStrLn "Enter the number"


storePath :: FilePath
storePath = "../store/words"

getWords :: FilePath -> IO [Translation]
getWords path = do
  file <- readFile path
  let line = lines file
  
